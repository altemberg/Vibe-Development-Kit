# Start Card Command

Use this command to start a card.

Steps:

1. Read `CLAUDE.md`.
2. Read the selected card from `cards/`.
3. Determine complexity.
4. Select the appropriate pipeline.
5. Create artifacts under `artifacts/output/`.
6. Create a minimal handoff under `artifacts/input/` for the next agent.
